# Core dependencies for Streamlit deployment
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.21.0
plotly>=5.0.0
folium>=0.14.0
streamlit-folium>=0.13.0

# Data generation
Faker>=18.0.0

# Basic utilities
python-dotenv>=1.0.0

# Optional: Remove heavy dependencies that cause deployment issues
# kafka-python==2.0.2  # Not needed for basic Streamlit deployment
# pyspark==3.4.1       # Too heavy for Streamlit Cloud
# apache-airflow==2.6.3 # Not needed for basic deployment
# prefect==2.11.3      # Not needed for basic deployment
# google-cloud-bigquery==3.11.4  # Not needed for SQLite version
# fastapi==0.100.0     # Not needed for Streamlit-only deployment
# uvicorn==0.22.0      # Not needed for Streamlit-only deployment
# pytest==7.4.0        # Not needed for deployment
# pdfkit==1.0.0        # Requires external dependencies

# Keep lightweight alternatives
fpdf2>=3.0.0
requests>=2.25.0